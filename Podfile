platform :ios, '9.0'

targetArray = ["RxSwiftExercise", "TableViewDemo", "LoginAndRegisterDemo"]
targetArray.each do |t|
    target t do
      use_frameworks!
      pod 'RxSwift',    '~> 3.0'
      pod 'RxCocoa',    '~> 3.0'
    end
end
